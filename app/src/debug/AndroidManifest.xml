<?xml version="1.0" encoding="utf-8"?>
<!-- Debug builds only: allow cleartext to any host so physical devices can
     hit the local dev API via the Mac's LAN IP (e.g. http://192.168.x.x:8000).
     Release keeps the strict res/xml/network_security_config.xml. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application android:usesCleartextTraffic="true" tools:node="merge" />
</manifest>
